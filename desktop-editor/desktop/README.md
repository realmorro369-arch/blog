# Windows 桌面构建

在项目根目录执行以下命令，即可生成免安装的 Windows 单文件应用：

```bash
pnpm desktop:win
```

完成后，`release/windows/` 内会出现 `正文校样室-Setup-1.0.0.exe`（实际文件名按打包工具输出为准）。该版本完全离线运行，正文草稿只存储在应用所在设备的本地浏览器存储中。

开发时可先运行 `pnpm dev`，浏览器会显示同一份本地预览界面。
